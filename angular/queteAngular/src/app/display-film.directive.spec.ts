import { DisplayFilmDirective } from './display-film.directive';

describe('DisplayFilmDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayFilmDirective();
    expect(directive).toBeTruthy();
  });
});
